# Copy to scripts/rag/.env and fill in. NEVER commit the real .env.
# Service-role key bypasses RLS — server-side tooling only.
SUPABASE_URL=https://bsuixyulutyiqmjahzvg.supabase.co
SUPABASE_SERVICE_ROLE_KEY=
VOYAGE_API_KEY=
